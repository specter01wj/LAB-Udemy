show databases;

create database sec5_test;

use sec5_test;
use jw_coffeeStore;

show tables;

describe products;

select * from products;

insert into products (name, price, coffee_origin)
values ('Mocha', 5.75, 'USA');

insert into products (name, price, coffee_origin)
values ('Latte', 6.75, 'China'), ('Cappuci', 7.75, 'Japan');


update products
set coffee_origin = 'UK'
where id = 3;

set sql_safe_updates = 0;

update products
set price = 3.55, coffee_origin = 'China'
where name = 'Latte';

update products
set coffee_origin = 'Japan'
where coffee_origin = 'UK';


drop table people;

create table people (
    id int auto_increment primary key,
    name varchar(30),
    age int,
    gender enum('M', 'F')
);

select * from people;

insert into people (name, age, gender)
values ('James', 23, 'M'), ('Kim', 25, 'F'), ('Lee', 26, 'F');

delete from people
where name = 'Kim';









