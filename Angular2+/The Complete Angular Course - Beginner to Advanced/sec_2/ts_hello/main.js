/*function log(message) {
    console.log(message);
}

var message = 'James Wang';

log(message);*/
var drawPoint = function (point) {
    console.log(point.x * point.y);
};
drawPoint({
    x: 3,
    y: 12
});
// Classes
// Objects
